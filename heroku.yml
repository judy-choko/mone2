setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
    image: web
    command:
      - python -c 'from app import init_db; init_db()'
run:
  web: gunicorn -w 4 -b 0.0.0.0:$PORT --log-level debug app:app
